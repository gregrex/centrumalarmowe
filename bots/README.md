Bots: placeholder README

Cel: prosty AI bot do wypełniania brakujących graczy (role-based behavior).
Plan minimalny:
1. Implementacja skryptu symulującego decyzje (node/python) opartego na mission_schema.
2. Podłączenie bota do `server/` jako klient WebSocket (auto-join + role selection).

Mogę wygenerować przykładowy bot-client w Node.js jeśli chcesz (domyślnie: tak).