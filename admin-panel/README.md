Admin-panel: skeleton placeholder (React rekomendowany).

Kroki początkowe:
1. `npx create-react-app admin-panel` lub użyć Vite + React.
2. Zadania: upload mission JSON, list missions, enable/disable, session debug view.

Mogę utworzyć minimalny `package.json` i komponenty jeśli potwierdzasz React.